services:
  omnitrack:
    build: .
    container_name: omnitrack-datalayer-studio
    restart: unless-stopped
    ports:
      - "3010:3010"
    environment:
      - PORT=3010
      - NODE_ENV=production
